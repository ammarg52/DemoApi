apiVersion: v1
kind: Service
metadata:
  name: demoapi-service
  labels:
    app: demoapi
spec:
  selector:
    app: demoapi
  ports:
  - name: http
    protocol: TCP
    port: 8090
    targetPort: 8090
  type: ClusterIP
